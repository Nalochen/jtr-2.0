openapi: 3.0.0
info:
  title: Customer Frontend API
  version: 1.0.0
paths:
  /api/user-frontend/get-user-details/{userId?}:
    get:
      summary: Get user details
      operationId: getUserDetails
      parameters:
        - name: userId
          in: path
          required: false
          schema:
            type: number

      responses:
        '200':
          description: OK
