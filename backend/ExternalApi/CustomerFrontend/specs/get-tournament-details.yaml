openapi: 3.0.0
info:
  title: Customer Frontend API
  version: 1.0.0
paths:
  /api/customer-frontend/get-tournament-details:
    get:
      summary: Get tournament details
      operationId: getTournamentDetails
      parameters:
        - name: tournamentId
          in: query
          required: true
          schema:
            type: string

      responses:
        '200':
          description: OK
